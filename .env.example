# Base URL for the Go API.
VITE_API_BASE_URL=http://localhost:8080

# Maximum number of listings allowed in a single import upload.
# Must stay in sync with the backend's MAX_IMPORT_ROWS limit.
VITE_MAX_IMPORT_ROWS=5000
